name: Build Tauri App

on:
  workflow_dispatch:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a message
        run: echo "Hello, this is a message from the GitHub Actions workflow."
